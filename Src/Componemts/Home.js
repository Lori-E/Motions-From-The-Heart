import React from "react";

const Home = () => (
  <main style={{ padding: "1rem", textAlign: "center" }}>
    <blockquote style={{ fontStyle: "italic", color: "#D6336C", marginBottom: "1rem", fontSize: "1.25rem" }}>
      "There are no better gifts to be given than gifts from the heart"
    </blockquote>
    <p style={{ fontSize: "1.125rem" }}>Welcome to MOTIONS FROM THE HEART, where every gift is made with love and meaning.</p>
  </main>
);

export default Home;
